[project]
name = "space2farm"
version = "1.0.0"
description = "Space2Farm - Agricultural Drought Early Warning Dashboard"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "flask-cors>=4.0",
    "numpy>=1.24",
]

[tool.vercel]
entrypoint = "dashboard/app:app"
